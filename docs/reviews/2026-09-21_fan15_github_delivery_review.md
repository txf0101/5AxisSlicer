# FAN15 四例 GitHub 交付复盘

2026-09-21 按用户要求以当前基本可用的离线版本收口。交付范围为弯管、球形 NEU 校徽、叶轮和三叶扇：各自的 STEP 模型、原始 G-code、新算法 G-code，以及真实 NC 回读对比图、生成路径压缩数据、回读路径数据和原有检查报告。文件入口集中在[示例索引](../../example/README.md)。选用最终弯管 v5，未加入早期重复 NC、测试缓存、性能实验与失败工件。

四份新代码的 SHA-256 与[离线验收清单](evidence/2026-09-20_fan15_repairs/fan15_offline_acceptance_manifest.json)一致；模型及旧代码与各例报告的源文件哈希一致。先前离线验收覆盖完整 NC 流回读、独立逆运动学检查和路径图。本轮仅做交付内容与哈希核对，不把这次打包称为新一轮切片验收。大文件使用 Git LFS，保留原目录名便于将来复核和再生成。

保留的限制：IPW 碰撞检查按用户此前指示暂缓；机床标定、轴动态、控制器宏、材料承接和试打没有资格。三叶扇旧代码的 B 轴仍是待确认的旧机床语义。现有 GUI 选边至四例导出的完整矩阵尚未逐例走通。后来需要制造发布时应从这些缺口继续验证，不能凭当前离线代码直接上机。
