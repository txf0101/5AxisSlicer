# 2026-07-26 成果预览导出功能清理复盘

## 1 用户判断与核对依据

用户在成果页复核中明确提出，论文图导出卡片无需进入软件功能。该卡片包含 4K 预设、输出目录、中英文切换导出及审计文件提示，与当前管状切片开发主线没有直接关系。本轮据此收紧成果页职责，保留模型和路径检查，移除论文成图链路。

修改前核对了 `圭臬/开发目标文档.docx`。文件大小为 492214 字节，修改时间为 2026-07-06 13:45:16；本轮保持只读。2026-07-21 的论文级成果页复盘继续保存历史实现和验收事实，本轮复盘记录后续产品边界，不改写旧记录。

提交前的最终差异涉及 25 个文件，新增 101 行、删除 4555 行，另删除一个二进制参考图。新增内容集中在负向回归、当前结构说明和本复盘；删除量主要来自成图模块、四面板脚本及其专项测试。

## 2 清理范围

软件内论文图导出按完整调用链清理：

- 成果页移除 4K 论文成果页、输出目录和两组导出按钮，三栏布局同步收紧；
- 主窗口移除导出队列、交互锁、状态反馈和相关生命周期编排；
- 自动化路由删除 `/results/export`，成果状态不再暴露导出字段；
- 删除 `paper_export.py`、论文导出本地化文本和专用样式；
- 删除四面板脚本、包内参考图、资源定位模块及专用测试。

成果预览页面继续承担 G-code 与 STEP 加载、工件坐标路径显示、阶段进度、源文件统计、缩略图、真实代码上下文和性能检查。交互质量档继续服务日常查看；“完整路径 / Full Path”读取完整正挤出 timeline。自动化接口保留 `mode="paper"` 兼容值，界面及文档统一解释为“完整路径”，该值不生成文件。

## 3 架构判断

原 `paper_export.py` 有 1311 行，四面板脚本有 1631 行。它们还牵引主窗口导出状态、Qt 捕获、OpenGL FBO 合成、PNG 元数据、跨进程锁、成对提交、包资源和大量专项测试。论文插图已经完成，继续在制造软件内维护这条链路会扩大页面状态与发布边界，后续坐标、分块和路径生成开发也要持续理解无关上下文。

四面板脚本直接导入 `five_axis_slicer.paper_export`，其运行依赖软件内部 Qt 成图合同。保留脚本需要继续维护被删除的导出模块，或复制同等规模的合成逻辑。两种方案都会留下重复职责。本轮连同参考资产和专用测试一并移除，使成果页代码只围绕加载、展示和检查组织。

这次清理形成一条可复用判断准则：产品模块应对应持续存在的用户任务，并拥有清晰的输入、状态和验收边界；一次性交付脚本若与应用生命周期深度耦合，应在任务结束后连同入口、状态、资源和测试整体评估。该准则适用于后续 Planar、Rotary、Tube 与 Freeform 工作台的功能取舍。

## 4 验证记录

定向 Qt 回归共 19 项，覆盖成果页布局、完整路径质量、自动化状态和主窗口交互，结果全部通过。静态质量门中的 Ruff、Security Ruff、格式、上下文预算和 Mypy 均通过；`compileall` 未发现导入或语法问题。全仓回归结果为 `279 passed, 2 skipped`，两项跳过仍来自 Windows 符号链接权限。

1600 × 900 离屏截图确认右栏由状态、统计、缩略图和代码区连续组成，没有导出卡片或空白占位。该环境缺少可用的硬件 OpenGL 像素格式，VTK 中央画布无法形成有效场景；Qt 页面尺寸、滚动范围和控件布局已完成检查，真实三维画面继续由既有 OpenGL/VTK 回归覆盖。
