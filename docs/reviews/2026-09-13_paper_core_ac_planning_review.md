# 论文核心 AC 范围调整复盘

日期：2026-09-13；任务 PC00。用户决定资源优先用于论文半球、扇叶、弯管、叶轮及材料切换，明确要求批判性核对论文。已比对用户原 DOCX 与仓库存档哈希、正文和表格、当前工具路径/后处理/工作台目录、自有机型配置、Tube 待验证行及已有扇叶量测报告。

范围、论文差异、开源固定版本与采用决定统一见[核心范围说明](../planning/paper_core_ac_scope.md)，状态只在[台账](../planning/progress_tracker.md)维护。新增 PC00—PC07，长期 F/X/I 原编号保留；PC02 只交付四例需要的 Freeform 子集，不能替代 F06。现有基础以复用为主，不因论文使用“freeform”而启动通用场优化。

关键判断：Curve 单条边、Rotary 圆柱轮毂通过均不能证明整片叶片覆盖；旧 NC 和历史实测不能证明当前版本自动生成。绝对/相对 E、Z=20 与 Z+20、位置范围与累计绕线、4/8次选择与实际转换分别建立验收口径。README 原 Tube 全完成声明与台账待验证冲突已更正；运行时首页旧标签交 PC05 实施，不在文档任务中改产品代码。

开源候选实查 README/许可、固定 commit 与相关入口；Open5x/COMPAS 为 MIT 部件候选，优先现有 OCP/OCCT 和 CadQuery 真值，Clipper2 备用；COMPAS 的 LGPLv3/CGAL 下游许可不能被 MIT 项目名覆盖；Klipper/Happy Hare 用于接口与宏流程参考；MAGE 的 Noncompete 排除本版直接移植。没有下载整个第三方项目、安装依赖或引入实现。Siemens 公开页返回200，私有手册没有作为已读资料。

本轮只修改规划、资料、README 和索引；静态检查结果为主表64行、PC任务8行、延期16行、待验证4行，任务编号无重复，PC依赖全部存在，7份变更文档的本地相对路径无缺失，`git diff --check`通过。检查脚本首次未识别AUD-02中的连字符，修正编号匹配后通过；该错误不涉及产品代码。未运行算法/Qt/全仓测试，未验证实机或实验原始数据，未改论文原件。证据采用少量文本和固定 URL，避免增加大体积中间产物。模型为本任务当前配置；精确 token 不可得，不据字数估算。

使用的 Skills：`five-axis-workbench-development`，以及 `references/stage-gates.md`。Skill 促使保留原阶段门、独立真值、共用 Toolpath 和实际来源记录，并将论文核心离线交付与完整工作台/实机资格分开。
