# FAN15 修复过程记录

继续工作先读 [执行计划](../planning/fan15_repair_execution.md)，状态以台账为准。历史四例审查目录保持不变，新证据写入 `evidence/2026-09-20_fan15_repairs/`。

## 2026-09-20 公共材料域与 Tube 方向

- 当前实现先按每个实体恢复含孔材料截面，再对截面材料面做并集，保留原 CAD 引用。最初整实体 Fuse 方案在叶轮接口产生额外分支，`impeller_probe01/02.json` 保留失败，已改为截面材料并集；`impeller_probe03.json` 的 Z23.4 通过，原 Z2、Z2.2 截交缺陷仍拒绝。多个材料必须分操作。依据：[OCCT 官方布尔运算说明](https://github.com/Open-Cascade-SAS/OCCT/wiki/boolean_operations)，复用现有面布尔工具，没有复制第三方代码。
- 轮廓嵌套判断改用轮廓上的点检测其他不相交边界，避免凹轮廓的顶点均值落在别的材料岛内。新增嵌套、相交、接触、分离、孔、孔内岛、凹形分离反例。
- 公共 feature fill 不再吞掉偏置异常，不再把原始 CAD 边界作为中心线回退；过窄无中心线的区域明确拒绝。此行为修复漏检，尚未实现可变道宽适配。
- 带孔域的内缩改为外域内缩减去孔域外扩，避免薄环相遇被整体偏置内核误报；各边界偏置或布尔失败仍拒绝。弯管 Z29、Z31 原有两类失败通过，全部 55 个历史失败高度已复测（整实体并集版本）；较高位置开口状材料域仍有偏置自交/失败。Z41.8 用 0.001 mm 轮廓简化也未修复，未采用该试验作为生产回退。
- Tube Continuous 的管壁径向仅定位路径，喷嘴轴和沉积面方向跟随中心线切向；起止抬离也沿该方向。竖直直管 -Z 和 90° 弯管末端 -Y 解析测试通过。保留人工径向轨迹测试验证旋转不连续拒绝，不把原错误方向造成的 C 绕转当成新轴向工艺要求。
- Planar 产品版本升级 v4，Continuous 产品升级 v2，避免旧资格被视为新算法结果。

## 弯管完整壁候选的进一步纠错

选择已有 Tube Buildup 多道能力，底座使用共享壳层/皮肤/20% 内填充。1 mm 管壁取三道等宽 1/3 mm，作为待工艺标定的几何候选。发现浮点比值略大于整数导致额外一整道，初次实际生成四道、体积 8631.0206 mm³；修正取整后恢复三道，独立反例通过。

首张完整候选图仍有明显分度层组接缝。定位到 Buildup 复用 Indexed 的分组固定平面，改为每层垂直实际管轴，层间连续改变方向；曲段按曲率减小轴向增量，使外弯层高及内外侧变化受参数限制。局部道厚采用扫掠 Jacobian `1−κ·radial`，逐段材料不再假定整个环等厚。直管末层保持实际厚度，G1 不连续或曲率过高明确拒绝。

新候选为底座 25 层、管壁 512 层、每层三道。指令管壁体积 6471.7529 mm³，CAD 为 6473.0720 mm³，相差约 −0.0204%；这只验证总量，不能替代局部覆盖和承接。Buildup 产品版本升级 v3。`pipe2_candidate` 保存原四道、固定分组及当前轴向候选数组/报告，未输出可上机 NC。

## 已执行验证与失败记录

- 预检：项目 `tmp/pytest9`，依赖和 QSettings 可写通过。
- 首次新测试单独收集缺少 `PYTHONPATH=src`，补齐环境后运行；保留 `r01_first.xml`。
- 新相交方块测试预期误写 108，独立重新计算为 100+16−12=104；修正测试真值，保留 `r01_retry.xml`。
- 第一阶段 18 passed、1 xfailed，见 `r01_analytic.xml`；此时 Continuous 未修复。
- 中间 Planar 回归 242 passed，见 `r01_planar_regression.xml`；发生后续偏置/版本修改，需要最终回归替代资格。
- 修复喷嘴方向后原 C 绕转断言失效；改为独立径向反例，并新增轴向 C 固定和弯曲末端方向真值。`direction_fixed.xml` 为 23 passed，无已知失败豁免。
- 带孔偏置第一轮相关测试 35 passed，见 `offset_first.xml`；定点弯管复测见 `pipe2_probe02.json`。
- 校徽定点复测中 Z21.4、Z21.6 通过；Z23.4、25.6、28.4 仍有过窄/偏置错误，见 `hemisphere_probe01.json`。该次测试早于带孔偏置修改，需按新版本继续复查。
- 截面并集及偏置后的相关回归 `combined02.xml` 为 262 passed；晚于此的 Buildup 轴向修改另进行全仓回归。薄环解析真值与选边绑定流程 `ui_bind_first.xml` 为 4 passed。Buildup 原相关矩阵 8 passed，新增弯曲层方向、局部高度和解析体积真值 2 passed，分别见 `axial_buildup_first.xml`、`axial_truth.xml`。

## 真实电脑操作

最终全仓运行 `full01.xml` 为 913 passed、3 skipped、1 failed，另有 141 subtests passed。失败位于 Continuous 产品测试：仍断言 v1 版本；改为 v2 后进一步发现旧断言预期 ready，而轴向直管 A=0 正确产生 `xyzac.rotary_singularity` warning 并保留 C。检查导出的 warnings.json 后，测试改为明确要求 warning 且告警集合仅包含该码，未放宽生产检查。`product_fix02.xml` 全部 5 passed。未再次运行全仓；此次后续修改仅为上述测试断言和 Buildup 局部变量类型标注。Ruff、格式、上下文预算及 Mypy（173 源文件）通过。

当前真实沉积路径对比图为 `evidence/2026-09-20_fan15_repairs/pipe2_candidate/comparison_candidate.png`，已目视检查。生成脚本同时保存 base_toolpath.json.gz、wall_toolpath.json.gz，含完整点属性和事件；这两份独立路径尚未合并或获得机床资格。

使用 `computer-use` 的 `sky` 接口读取应用、启动后的切片窗口，实际点击管状工作台与“打开 STEP”，截图已在本会话呈现。文件对话框的输入控件在状态树中可见，但两次 `set_value` 返回 `element 294 is not available in cached app state for python.exe`；刷新与快捷键后也未确认文件名焦点，未输入模型路径。未宣称已完成真实鼠标选边。软件测试实例位于本机端口 8766，未连接机器。

## 下一步（每次继续须回读）

### 2026-09-21 自动底座根因修复

发现 Tube 产品链 `_base_from_substrate` 根据包围盒猜圆形实体，真实孔与非圆外形可能丢失。已替换为共享 `algorithms/planar/substrate.py`：真实 CAD 截面、共享材料域、壳层/皮肤/填充、规范 Toolpath，再由原产品链合并。底座参数为两道壁、20% 填充、上下各四层实心皮；道宽/进给沿用 Tube 操作。均分实体厚度避免末层超挤出。保留显式解析圆底座接口，自动几何当前仅支持已建立 +Z 生长姿态，其他姿态拒绝，未假装通用坐标支持已经完成。Buildup 产品升级 v4，使旧资格过期。

新增带孔矩形解析反例，核对矩形角部、孔内无跨越沉积、全高和末层厚度；另测试产品入口与不支持姿态拒绝。`cadbase02.xml` 为 9 passed；Ruff/格式/Mypy（174 源文件）通过。真实弯管改用同一产品底座入口重新生成，结果保存在 `pipe2_cadbase_20260921/`，旧 `pipe2_candidate/` 保留。此项属于共享算法和产品链修复，没有更改预览几何来掩盖错误。校徽、叶轮及三叶扇整件生成仍待实现/验收，不能据此标记全部修复。

本轮 Tube/Planar 相关回归 `cadbase_regression.xml`：427 passed、494 deselected、11 subtests passed，未运行第二套全仓。真实弯管新产物为 26 层底座、512 层管壁，图片已检查，完整点/事件分别存于两个压缩 Toolpath JSON，尚未合并取得制造资格。

### 2026-09-21 球面整实体层域

新增共享领域模块 `algorithms/freeform/spherical_domain.py`。对完整实体与同心球面求交，恢复所有材料岛与孔；不再依赖有限 guides。上半球图表显式给出位置、法向、长度度量和面积 Jacobian；球面不能等距展平，平面面积禁止直接当材料量。边界面积以有向球面多边形积分，孔保留负向贡献；仍需离散收敛核查。参考 [OCCT 球面定义](https://dev.opencascade.org/doc/refman/html/class_geom___spherical_surface.html) 与 [PROJ 投影文档](https://proj.org/en/stable/operations/projections/aeqd.html)，实现未复制第三方代码，也未新增依赖。

解析测试覆盖非零球心、奇异输入拒绝、带孔球面截交、球冠独立面积真值，`sphere03.xml` 为 6 passed。首次断言把 OCCT 近似截交误当成 1e-6 mm 精确值，`sphere01.xml` 保留；后续明确使用领域已声明的 0.001 mm 几何预算，不改生产容差。质量检查通过，Mypy 175 源文件。

真实校徽全部 37 个凸起实体在 R40.083333、40.25、40.416667 mm 共 111 次截交均恢复，无失败和遗漏实体；最大逐实体曲层积分/CAD 体积偏差 0.24777%。证据 `spherical_domain_20260921/report.json` 和 `boundaries.npz`。这些半径是 0.5 mm 凸起的域积分采样，未声明为喷嘴尖端高度；图为截层边界，尚无有限道宽填充、半球基底、空移与完整 NC，不恢复制造资格。

球面与 Freeform 相关回归 `sphere_regression.xml` 为 17 passed、910 deselected。`spherical_domains.png` 已目视检查，37 个凸起图案完整可见；该图仅表示真实截交边界。

2026-09-21 用户补充：机床参数沿用 example 论文，目标物理轴 AC，G-code 默认 AC，可自定义输出轴字；缺项允许参考默认值。论文表 1 明确 A 绕机器 X、C 绕转台 Z，A 范围 ±180°、C 范围 ±360°，喷嘴 0.4 mm、丝径 1.75 mm。软件已有旋转轴字映射接口及保持运动学不变的测试。旧 B 暂作为 C 输出别名假设核对，仍需检查符号、零点和宏造成的位移；无需等待额外配置才继续开发。论文的绝对 Z20 仅为该原型换姿安排，不能直接当作任意模型的防碰撞保证。

下一步：球面有限道宽偏置/填充需采用真实度量，逐实体核查覆盖与首层贴合；随后接 Toolpath 和工作台共享命令。弯管继续检查内外弯层间承接、底座连接和产品链。叶轮整实体曲层域及三叶扇分区仍待实现。四例完整 NC 验收均未完成。

使用 Skills：five-axis-workbench-development、five-axis-slicer-validation、computer-use。Skills 要求的材料承接、独立真值、版本过期与真实操作证据已纳入计划；离线几何修复不能恢复整机制造资格。

### 2026-09-21 真实选边与产品入口复测

Computer Use 刷新窗口后可恢复操作；原生文件对话框通过 Alt+N 聚焦文件名、输入路径并确认，已实际加载弯管 STEP。首页打开 STEP 进入结果查看器，制造工作台内打开 STEP 才绑定制造模型，测试必须区分这两个入口。

发现 Tube 操作原先只有几何下拉框，没有针对角色的鼠标拾取入口。已在管体、入口、出口、基体各增加拾取按钮，使用现有 PickRequest/PickHit；限制候选类型，拒绝未知引用，先更新草稿，应用后提交。真实窗口已分别鼠标选中 body_002、入口 0003、出口和 body_001 并应用。首次选中出口内圆 0012，实际生成返回 tube.port_not_outer_boundary；重新鼠标选中外圆 0014 并应用后重新生成。未将内圆强行解释为外圆，也未放宽几何检查。

另修复错误详情被通用“生成失败”文字覆盖，以及操作标题始终显示 Indexed。新增失败详情回归通过，导出按钮继续禁用。拾取初轮 4 passed（pick01.xml），错误详情与拾取复测 2 passed；Ruff/格式/上下文预算/Mypy 175 源文件通过。Tube UI 壳层预算为四行拾取路由和状态清理增加四行，实际逻辑置于 tube_operation_ui，未提高算法验收容差。

scripts/fan15_live_pipe_setup.py 通过共享命令配置真实鼠标选择后的 Setup 与 Buildup；请求记录在 evidence/2026-09-20_fan15_repairs/live_pipe_pick/commands.json。0.4 mm 喷口按论文，外包络使用明确的离线测试夹具参数；机型保留 reference_only，不能据此声称实机资格。当前外圆重试进入长时间计算，窗口生成按钮禁用，HTTP 状态查询超时；还需定位长阶段缺少事件泵/取消检查的问题。四例完整 NC 验收仍未完成。

长任务响应第一步：Buildup 的碰撞检查每 64 段调用取消检查点，让控制器能处理事件并传播取消；未减少采样、碰撞对象或通过门槛。新增无回调/有回调完整结果一致及取消异常传播测试。质量检查通过；原已运行实例未热加载这一修改，真实取消响应仍需在新实例验证。逆解等其他长阶段也需要继续核查，不能把该修复视为整个生成流程已经可响应。

本轮最终相关回归 pick03.xml：36 passed、2 subtests passed（478.65 s），覆盖 Tube UI 与碰撞索引/取消检查。真实生成实例端口 8767 仍未返回，末次 HTTP 查询继续超时，完整 NC 未生成验收。下一步需在可观测的新测试实例中定位逆解/碰撞/序列化耗时，重测取消，不反复点击已禁用的生成按钮。

### 2026-09-21 长生成定位与真实整件重切

以提交 0d50c5e 为起点，旧测试实例累计 CPU 超过 2,600 s 仍未响应，保留几何/参数请求记录后结束该测试进程。新增 diagnose_pipe_product.py，直接走实际产品生成入口，对真实弯管逐阶段计时，不用小型解析夹具替代。首次 faulthandler 在本机解释器 shim 中仅留下截断栈并退出，改用阶段计时，旧 stacks.txt 保留。

定位：完整管壁约 35.7 s、CAD 底座约 0.34 s、合并约 3.3 s；逆解约 212—213 s，之后碰撞检查继续长时间计算。几何生成、逆解原先均无长循环事件检查；碰撞粗网格候选中大量线段未进一步筛除，逐条 Python 距离计算随已打印历史增长。

修复：Buildup 每层检查取消，XYZAC 每 64 点检查取消；每次逆解调用内部使用有界 A/C 工件变换缓存，保留独立逐点 FK；碰撞索引先做保守包围盒和带余量的批量距离筛选，最终命中仍用原 scalar 判断，并保持最早命中顺序。没有缩小喷嘴、删碰撞对象、减少运动采样或提高通过容差。当前参考喷嘴包络仅为离线夹具，非实测。

证据：真实前 3,000 点 old/new 完整碰撞结果一致，15,678 采样、14,858 条原始诊断不变；最终批量版本约 2.38→1.76 s。逆解对照 3.13→1.70 s，完整轨迹完全相等。完整新运行逆解 118.76 s。见 pipe_profile_20260921 的 benchmark JSON 与 pipe_profile_final_20260921 的阶段记录。随机/退化/长线段保守筛选测试、回调输出不变和取消传播通过。perf05.xml 为 88 passed、14 subtests；其后的层生成回调与产品相关集 perf08.xml 为 10 passed。新增测试两次失败分别为夹具使用正坐标机型、默认道间距大于测试道宽，已修正夹具，生产限制未放宽。

完整合并路径已实际生成并保存：208,232 点，201,882 段沉积、6,349 段非沉积。pipe_profile_optimized_20260921/full_product_paths.png 已目视检查，图来自实际 Toolpath，分别显示全体沉积与空移，无抽段、无 CAD 外形补画。此时完整碰撞验证仍在运行，不等于合格 NC；新执行计划要求每次相关算法修改后必须重切完整示例、看全体路径并按门槛回读，不能仅凭单测或外形关闭任务。

最终产品失败返回修复：Buildup 在首次碰撞段后及时返回 Error，报告明确 collision_check_complete=False，后续段不冒充已检查；底层诊断接口默认仍可全量扫描。没有碰撞时仍检查全程。即使 issues 为空，显式未完成的报告也禁止导出。GUI 同时显示产品验证错误码和检查未完成标记，不只显示泛化失败提示。此改动减少已知错误后的等待，不改变可通过路径的检查要求。

实际完整重切结束于 pipe_product_failfast_20260921：几何/底座 36.24 s、合并 3.26 s、逐点逆解 120.59 s、验证 0.024 s、收尾 0.011 s（核心阶段约 160 s，不含诊断文件压缩开销）。全路径 208,232 点，最终 Error，ready_for_export=False；碰撞检查在 24 次运动采样后返回首个失败：op01-point-0000005 对此前 op01-point-0000002，segment_index=3，位于底座首圈 Z≈0.192308 mm。参考喷嘴 RZ=(0.2,0),(3,2),(3,12.5)，现检查器用球体保守近似，必须进一步区分近似误报和真实干涉；禁止缩小该包络来取得通过。NC 未生成，回读未执行，后续碰撞未完成。完整图 full_product_paths.png 已按此次实际 Toolpath 重新绘制并查看。

最终相关回归 perf11.xml：153 passed、9 subtests passed（43.78 s）；UI 错误展示及失败早退/回调专项 perf10.xml：10 passed。Ruff、格式、上下文预算和 Mypy 175 文件通过。没有再次执行全仓，也没有新实例原生 UI 长任务取消验收；核心取消传播和路径不变有测试证据。使用 Skills：five-axis-workbench-development、five-axis-slicer-validation，要求真实重切、独立对照、拒绝状态和未执行检查分别留证。

下一步：保留本次首个碰撞段作独立反例，核对完整 RZ 喷嘴包络与已打印材料的真实几何；修正后重切完整弯管，再做允许导出后的控制器回读与原生 UI 取消/错误恢复验证。其余三例及四例完整 NC 验收仍未完成。

### 2026-09-21 碰撞暂缓后的离线 NC 闭环

用户明确暂缓碰撞问题，优先完成路径与 NC 检查。使用 Skills：five-axis-workbench-development、five-axis-slicer-validation。范围修改已进入执行计划，生产碰撞检查没有删除或缩小喷嘴；诊断脚本显式 `--defer-ipw` 调用现有参数，另存检查范围与 OFFLINE 文件。`collision_check_complete` 在此只涉及配置的检查范围，不代表 IPW 已检查；以 `inspection_scope.json` 中 deferred 为准。

弯管从真实 STEP 完整重切，37.37 s 规划、3.33 s 合并、122.60 s IK、13.72 s 配置范围验证、21.35 s 后处理与回读。208232 点全部回读一致，坐标、进给、挤出、顺序错误均为零。证据 `pipe_offline_nc_20260921/`。独立零中心 AC 逆变换（扣除本次 12.5 mm 工具长度）核对全部 NC 点，最大误差 0.000001312 mm；据回读数据绘制 nc_comparison.png，旧数据缓存先核对原文件 SHA256，图保留 669892 旧沉积段、201882 新沉积段及 6349 新非沉积段。外形对照不能代替局部承接和覆盖验证。

新增共享 thermal_program 模块，显式温控等待在运动前、M400 后关闭加热。参数取已保存论文提取配置 own_ac_fdm.json 的 PLA 195/45 ℃，与早期计划起点 205/60 ℃区分；未重新声称本轮复核论文原文。命令语义核对 [Marlin M109](https://marlinfw.org/docs/gcode/M109.html) 与 [官方 G-code 索引](https://marlinfw.org/meta/gcode/)。不编造归零、调平或停车宏，保留设备已归零/标定前置条件，文件仍非实机放行程序。独立手写温控与篡改测试、既有全流回读共 62 passed（thermal02.xml）；首次测试收集因新测试缺少仓库 src 路径失败，补齐测试入口后重跑，生产算法未因此修改。质量门禁全部通过，Mypy 176 源文件。

新增模型旁 `example/pipe2/弯管_新算法_离线检查_20260921.gcode`，原文件未改；热处理包装后的全部运动文本与已回读 NC 完全相同，SHA256 与参数记录于 delivery.json。轴速度/加速度限值未标定、局部材料承接/覆盖及原生完整导出流程仍待验证。四例没有整体验收完成；继续按 R03/R04/R05 推进。

随后实际生成三例完整基体，校徽首轮报 `substrate contains an empty build layer`。根因是公共 substrate 在层顶截交，半球最高点退化为空截面。修为层厚中面截交、喷嘴置于层顶，末层厚度仍由全高等分；没有丢弃末层，没有扩大几何容差。这是阶梯近似，不宣称精确球面。独立半球末层及带孔基体、Tube 产品测试 10 passed（substrate_midpoint01.xml）。Tube Buildup 版本提升 v5，旧缓存不能继承资格。

真实三例新基体均完成：校徽 65564 点/201 层、叶轮 68198 点/221 层、三叶扇 19280 点/501 层，保存在 remaining_substrates_midpoint_20260921；这些仅是基体，不是完整零件。旧失败记录 remaining_substrates_20260921 保留。公共改变后重新生成弯管 v5，208232 点回读继续通过，模型旁新增 `弯管_新算法_离线检查_20260921_v5.gcode`；其 NC 哈希与上一版相同，符合本例底座为等截面体的情况。最终相关回归 172 passed、9 subtests（nc_v5_regression01.xml），质量门禁通过。

三叶扇旧 NC 第 36153 行含 `SET_GCODE_OFFSET Z_ADJUST=-1.5 MOVE=1`。依据 [Klipper 官方语义](https://www.klipper3d.org/G-Codes.html#set_gcode_offset)，将显式 Z 调整与未验证的启动宏区分。对 A 绕 X/Y、A/B 正负及是否应用此偏移做 16 组候选，抽样端点/网格距离只作筛选，再对各倾转轴最佳候选各取 72 点做独立 BRep 距离。Y 倾转、正 A/B、Z=-1.5 候选：CAD 距离中位 0.0248 mm/P95 0.1100 mm，Z 范围约 55.9—96.0 mm；X 倾转最佳候选中位 60.96 mm/P95 70.46 mm。证据 three_leaf_axis_hypotheses.json。它支持旧文件采用不同倾转坐标约定，不能直接 B→C 字符替换；不代表已标定旧机床，也不改变用户目标物理 AC。首次探针遇到 venv SciPy/NumPy ABI 不兼容，改用 NumPy 分块范围内直接距离计算，未安装或升级环境。

下一步：校徽球面图案需要有限道宽曲层填充，并量测与阶梯基体的接触；叶轮需要八片完整生长域；三叶扇按已得到的旧坐标候选核对层族/承接，再用目标 AC 重新求解，禁止拷贝旧轴值。碰撞继续按用户约定暂缓，不阻塞这些开发。四例完整 NC 检查尚未全部完成。

### 2026-09-21 四例完整离线 NC 验收

本轮使用 `five-axis-workbench-development`、`five-axis-slicer-validation`、`skill-creator` 和 `computer-use`。新增经验已写入 `five-axis-workbench-development/references/fan-failure-lessons.md`，Skill 用 `PYTHONUTF8=1` 通过 `quick_validate.py`。首次校验在 Windows 默认 GBK 下读取 UTF-8 失败，属校验器运行环境，未改文档内容规避。

#### 球形 NEU 校徽

新增 `algorithms/freeform/spherical_fill.py`，在球面真实度量上生成有限道宽壳与实心填充。全部 37 个凸起实体在 3 个径向层中均存在；最大横向间距 0.399655 mm，首层与 R40 基体数值缝隙约为零，指令材料体积 737.5221 mm³，CAD 792.8026 mm³，误差 6.97%。完整程序 76098 点全部回读，独立逆变换最大误差 0.000000856 mm。模型旁文件为 `example/球形NEU校徽/球形NEU校徽_新算法_离线检查_20260921.gcode`；证据为 `logo_offline_nc_20260921/`。

#### 叶轮

新增 `algorithms/freeform/surface_solid_fill.py`。每片叶片显式选定侧曲面、对面和受支撑根边，按面内物理距离布置路径，再在约 1 mm 厚度内生成 5 层。8 片叶片共 40 个厚度操作、613440 个特征点；最大间距 0.393963 mm，最大段长 0.198690 mm，根边到基体最大缝隙 0.009117 mm，指令材料体积误差 6.18%。合并后 681718 点全部回读，独立逆变换最大误差 0.000001156 mm。模型旁文件为 `example/叶轮/叶轮_新算法_离线检查_20260921.gcode`；证据为 `impeller_offline_nc_20260921/`。

百万点性能问题可复现：本例 IK 697.67 s，后处理与回读 122.92 s；正确性通过，性能不标记为已优化。

#### 三叶扇

发现该 STEP 的三片叶片与中心体均有约 0.499815 mm 缝隙，所以不能用叶片 CAD 根面直接声称承接，也不能复用另一扇叶模型的 R17.5 mm。新增 `algorithms/fan/radial_solid_fill.py`，从每片 root face 量出首个真实截面半径约 10.009827 mm，向 hub 搜索受支撑的有限道宽中心半径约 9.699530 mm，每片先生成 2 个根部搭接层。首层整条路径到 hub 最大距离 0.200002 mm，与 0.4 mm 道宽的半径预算一致。

三片叶片各 254 径向层，共 1015249 个特征点；有限道宽材料体积误差 4.59%，截面积分体积误差 0.167%。合并中心体后 1034535 点全部回读，独立逆变换最大误差 0.000000842 mm。新程序从喷嘴轴重新求解目标 A/C，没有将旧 B 直接改名。模型旁文件为 `example/三叶扇/三叶扇_新算法_离线检查_20260921.gcode`；证据为 `three_leaf_offline_nc_20260921/`。IK 939.24 s，后处理与回读 174.59 s，同样保留性能待办。

#### 合并链、质量门与未验证项

新增通用 `postprocessing/toolpath_sequence.py` 和 `thermal_program.py`，合并操作间回抽、安全离开/进入及操作切换，并对 PLA 195/45 ℃温控包装前后的运动文本做一致性检查。四例图均来自旧 NC 解析与新 NC 回读，不画 CAD 表面补外形。

最终相关回归 `fan15_final_regression.xml`：222 passed、28 subtests passed（3:16）；类型标注补齐后 6 项新算法测试再次通过。Ruff 通过，5 个新增核心模块 Mypy 通过。

Computer Use 按 Skill 恢复规则执行两次轻量重试和一次 JS 内核重置，三次均为 `nodeRepl.fetch request failed`，应用与浏览器清单为空。因此本轮无新的真实用户点击证据；早先弯管鼠标拾取和本轮 Qt 自动化回归保留为不同证据类型。IPW 碰撞继续标记 deferred，实际机床标定、动态限制、控制器宏和试切均未资格化。

### 2026-09-21 实体操作公共链与界面首例

依 `docs/planning/fan15_productization_plan.md` 的 FP01—FP06 实施。公共 Freeform 操作新增球面实体、受根边约束的曲面实体和径向实体，body/face/edge 使用稳定几何引用；参数持久化、重绑、Stale、撤销、脚本/HTTP 均经同一控制器和命令入口。基体由 CAD 截层生成，特征与基体合并后统一 AC 求解、温控、后处理和严格回读。回归脚本只选示例几何和配置，不再复制实体切片/NC 实现。公共产品链三个真实模型分别输出 76098、681718、1034535 点，完整回读通过；证据为 `evidence/2026-09-20_fan15_repairs/{logo,impeller,three_leaf}_public_product_20260921/`。这三套与旧离线 finalizer 产物分开，仍属参考机型 OFFLINE。

本轮新增项目保存重开反例：`surface_solid_fill` 保存成功，但默认加载器只按 `freeform_` 前缀选类，`load_project` 拒绝。公共加载器显式纳入三种实体操作后，真实叶轮 STEP 的 `substrate_body`、根边、曲面角色、厚度参数和语义哈希往返一致。GUI 选边测试检查 root edge 与两个选面映射；错误类型由领域选择校验拒绝。

Computer Use 此轮恢复成功。真实窗口载入校徽 STEP，建立球面实体操作并在界面应用角色/参数；第一次点击 Generate 因准备夹具使用与 Setup 不同的 ID，控制器阻止生成。修正 `run_fan15_solid_product.py --prepare-project` 从实际 Setup 绑定操作，再经原生文件对话框重开，界面生成状态 `warning`、路径覆盖校徽、导出可用。导出目录 `tmp/fan15-ui-logo-export/` 六件套完整：76098 点、76098 轴样本、全流 readback passed、NC 落盘 SHA256 `F6C9CCEB143233623CF9D302771CEC073B7620B4906B5DE8343AE91E5411C220`，清单 `machine_executable=false`。该目录为本机测试产物，不作为长期归档；旧窗口未关闭或篡改。

发现百万点状态/HTTP/清单曾再次嵌入全部 toolpath 与轴样本，三叶扇清单可达 1.7 GB。Freeform 结果摘要改为 schema 2，保留来源、数量、验证和回读，完整点列分别在 `toolpath.json`、`preview.json`、`machine_axes.csv` 和 NC 中。校徽 GUI 导出清单约 5.9 KB；导出仍有 72.6 MB 工具路径和 53.8 MB 预览，需后续按读者兼容性考虑流式/压缩，不声称性能问题已关闭。长时界面计算期间 UIA 无法读取控件，结束后恢复；需要独立进度与响应验收。

使用 Skills：`five-axis-workbench-development`、`five-axis-slicer-validation`、`computer-use`、`skill-creator`。Skill 新增项目加载分派、Setup ID 和大结果摘要等经验，UTF-8 模式结构校验通过。仍未完成 FP03 跨工作台作业组合、其余真实模型 GUI 全流程、全部角色的交互拾取和现场机床资格；目标模式保持 active，不提交最终 Git 版本。

### 2026-09-21 用户教程与阶段版本

按现有手册索引梳理出 [五工作台图文点击教程](../guides/quickstart_clickthrough_zh.md)：公共 Setup → Planar、Curve、Rotary、Tube、Freeform 分支 → 已有 NC 预览 → 状态/回读/六件套与保存重开。每节给当前界面按钮和几何选择的最短点击序列，复用已注明来源的项目截图，再链接专项手册。根 README、手册索引与学习总册都指向该路线，Research 未完成不编造教程。Freeform 实体模式沿用公共界面，只有校徽完成真实 GUI 生成和导出；旧有限导引截图未冒充新增实体模式。图文教程降低初次进入成本，详细参数和错误恢复保留在专项手册，避免复制不同文档中的全套数值。

项目切换时还发现旧校徽生成的 Viewer 叠加留在新叶轮 STEP 上，即使状态已回到 Draft。`FreeformPage.set_controller` 现在清除旧 G-code 路径叠加，Qt 项目切换回归通过。需在真实窗口中补录切换截图，并继续完成叶轮、三叶扇 GUI 全流程。按用户请求做阶段性 Git 提交，目标仍保持 active；大型生成证据不纳入本次源代码提交。

### 2026-09-21 叶轮实际 GUI 导出与公共链续修

用 Computer Use 对既有应用窗口实际操作 Open Project，载入 `tmp/fan15-ui-impeller-project/project.json`，进入 Freeform 并检查保存的八片角色，点击 Generate。生成中旧窗口曾被 Windows 判为未响应，计算最终完成；状态为 Warning，Export bundle 可用。经原生文件夹对话框导出 `tmp/fan15-ui-impeller-export/` 六文件：`main.gcode` 101849918 B、`toolpath.json` 650809490 B、`preview.json` 485102161 B、`machine_axes.csv` 72963656 B，其余为清单和警告。清单记录 681718 点、16343 事件，全流回读同数且通过；8 片、5 个厚度层，最大跨道间距 0.393963 mm、根边最大缝隙 0.009117 mm、指令体积与 CAD 差 6.18%。`offline_exportable=true`、`machine_executable=false`。预览共有 673526 段挤出和 8191 段空移；视觉密线需继续做局部几何核对，不能当实机验收。目标目录未跟踪，不提交大文件。

响应性修复在 `solve_xyzac_trajectory`、自有 AC 后处理、严格回读和导出前验证边界增加可取消检查点，回调沿用 Freeform 控制器的 Qt 事件泵。独立测试检查有无回调的 NC/回读一致，以及回调抛取消异常时中断。真实窗口是修改前已启动的进程，未将其本次成功生成误称为新检查点的 UI 验证。Tube 公共产品将已审核材料的喷嘴/热床温度传入统一温控包装，恢复原运动文本严格回读；控制器泛用 PLA 测试核对 200/60 ℃，直接产品测试核对论文案例 195/45 ℃。温控仅是离线程序的一部分，真实控制器宏与整机前置动作仍未核验。

本阶段新增真实三叶扇项目加载与角色恢复，实际生成已启动，导出/回读结果待核对。新增测试与质量检查结果在最终提交前补记。下一阶段继续 FP03 的跨工作台显式作业图、Tube 真实 GUI、全部代表窗口尺寸和新进程取消响应性；当前目标不关闭。
